Ride

Clojure IDE written in clojure.

Currelntly clooj sources are used.

See myns.clj to see the prototype.

TODO

*Reload only updated(changed source) functions.
*test the prototype by running and changing on-the-fly clooj inside my editor.
*show values of local bindings for  binding|let|...
*show values of local bindings for loops loop|recur|for|...
?use clooj stuff(syntax highlighting, file loading)
